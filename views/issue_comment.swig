[issue comment {{ action }}]
repository: <{{ repository.url }}|{{ repository.name }}>
sender: {{ sender.login }}
issue: #{{ issue.number }} <{{ issue.url }}|{{ issue.title }}>
comment: <{{ comment.url }}|{{ comment.id }}>
body: {{ comment.body }}

